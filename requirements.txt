# Astro(Dockerfile)·CI(Dockerfile.app) 이미지가 설치하는 런타임 의존성.
# pyproject.toml [project].dependencies 의 미러 — 직접 수정하지 말고
# pyproject.toml 과 함께 갱신한다 (CI uv-lock-check job 이 동기화 검사).
#
# lock 전핀 export 를 쓰지 않는 이유: astro-runtime 베이스 이미지는 이
# 런타임 의존성을 이미 전부 설치·제약하고 있어(protobuf<6, cryptography<49
# 등) transitive 전핀을 강제 설치하면 Airflow provider 제약이 깨진다.
# 범위만 선언해 베이스 설치본을 재사용한다 (PR #81 리뷰 검증: 전핀 시
# pip check 위반 20건, 범위 시 no-op).
pydantic>=2.6,<3
pyarrow>=15.0
google-api-python-client>=2.100,<3
google-cloud-storage>=2.10
gcsfs>=2024.0
tenacity>=8.2,<10
requests>=2.32,<3
# CTR Model Training (Issue #33)
duckdb>=0.8.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
PyYAML>=6.0
joblib>=1.3.0
typer>=0.9.0
