name: Release

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  release-info:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print release info
        run: |
          echo "Tag: ${{ github.event.release.tag_name }}"
          echo "Name: ${{ github.event.release.name }}"
          echo "Target: ${{ github.event.release.target_commitish }}"
          echo "URL: ${{ github.event.release.html_url }}"

      # TODO: GCP Workload Identity Federation 설정 후 활성화
      # 현재 WIF pool이 Autoresearch-infra 리포만 허용하므로,
      # infra 팀에 Autoresearch 리포 추가를 요청해야 함.
      #
      # - name: Authenticate to GCP
      #   uses: google-github-actions/auth@v2
      #   with:
      #     workload_identity_provider: projects/185508640491/locations/global/workloadIdentityPools/autoresearch-github/providers/github
      #     service_account: <service-account-email>
      #
      # - name: Configure Docker for GAR
      #   run: gcloud auth configure-docker asia-northeast3-docker.pkg.dev
      #
      # - name: Trigger Cloud Build
      #   run: |
      #     gcloud builds submit \
      #       --project ar-infra-501607 \
      #       --config ../Autoresearch-airflow/cloudbuild.yaml \
      #       --substitutions _IMAGE_TAG=${{ github.event.release.tag_name }},_AUTORESEARCH_REF=${{ github.event.release.tag_name }}
