"""에이전트 실험 상태 추적(가설 제출 → 진행 보고 → 최종 리포트) 모듈.

[파이프라인] 일일 폐루프(수집 → 피처 → 학습 → 추천 → action log) **밖**의
에이전트 실험 축을 담당한다. 사용자가 가설을 제출하고, 자율 실험 에이전트가
진행 상태를 보고하며, 종료 시 리포트를 남기는 경로다. 폐루프 각 단계를 실행하는
것은 이 모듈이 아니라 `src/pipeline`·`autoresearch.jobs`이고, 이 모듈은 그
실행의 **관측 기록만** 보관한다.

[제공 기능] 실험 하나의 수명(제출·진행 이벤트·최종 리포트)을 append-only
이벤트로 저장하고, 그 이벤트로부터 현재 상태를 파생해 조회·목록·대시보드로
제공한다.

[비책임] 실험 실행·스케줄링(에이전트 루프 소유), 모델 학습·평가 구현
(`src/models`·`src/pipeline`), 실험 결과 아티팩트 저장(MLflow —
`src/tracking`), 대시보드의 인증·노출 경로(`SKYAHO/Autoresearch-infra`의
oauth2-proxy·내부 ILB 패턴).
"""

from __future__ import annotations
